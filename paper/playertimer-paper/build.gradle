plugins {
    id 'java'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

dependencies {
    compileOnly "io.papermc.paper:paper-api:${paper_version}"

    implementation project(":common:playertimer-core")
    implementation project(":common:timer-core")
}
